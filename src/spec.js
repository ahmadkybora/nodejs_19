const AuthController = require('../app/Controllers/Auth/AuthController.js');

describe("login", () => {
  test("test login", async () => {
    
    const output = 4

    expect(2 + 2).toEqual(output);

  });
});